## Packages
date-fns | Formatting dates for match schedules
framer-motion | Smooth page transitions and goal animations
clsx | Class name utility (already in base but good to confirm)
tailwind-merge | Class name utility (already in base but good to confirm)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'DM Sans'", "sans-serif"],
  display: ["'Oswald'", "sans-serif"],
  mono: ["'JetBrains Mono'", "monospace"],
}
